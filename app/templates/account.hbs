{{fixed-top-header}}
<div class="container container--fixedWidth container--forContent">
  <a href="#" class="u-pull-right Button" {{action "invalidateSession"}}>Logout</a>
  <h2>Account</h2>
  <p>
    {{unbound fa-icon "github"}} <strong><a href="{{session.secure.user.githubUrl}}">@{{session.secure.user.login}}</a></strong> ({{session.secure.user.name}})
  </p>
  <hr>
  <h5>Current plan: {{session.secure.user.subscription.planName}}</h5>
  <div class="row">
    <div class="five columns">
      {{meter-bar count=session.secure.user.subscription.currentUsage total=session.secure.user.subscription.planUsageLimit}}
    </div>
    <div class="seven columns">
      <p>
        <strong>{{format-number session.secure.user.subscription.currentUsage}}</strong>
        / {{format-number session.secure.user.subscription.planUsageLimit}} visual diffs
      </p>
    </div>
  </div>
  <hr>
  {{#unless session.secure.user.subscription.isFree}}
    <div class="row">
      <div class="five columns">
        {{subscription-info-form subscription=session.secure.user.subscription}}
      </div>
      <div class="seven columns u-text-right">
        <br>
        {{stripe-checkout
          text='Update Credit Card'
          checkoutLabelText='Update Credit Card'
          forceCheckout=true
          plan=session.secure.user.subscription.plan
          price=0
          planName='Update Credit Card'
        }}
        {{cancel-subscription}}
      </div>
    </div>
    <hr>
  {{/unless}}

  <h4>Upgrade plan</h4>
  {{pricing-section}}
</div>
<div class="container container--marketing container--light">
  <div class="container container--fixedWidth container--forContent">
    <h2>Pricing FAQ</h2>
    <h3></h3>
    {{pricing-questions}}
  </div>
</div>
{{main-footer}}
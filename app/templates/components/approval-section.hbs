{{#if build.isFinished}}
  {{approval-button build=build}}
  {{#if build.isApproved}}
    <p class="u-pull-right">
      Approved by
      {{user-name user=build.approvedBy}}
    </p>
  {{/if}}
{{/if}}
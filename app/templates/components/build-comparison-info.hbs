{{fa-icon "compress"}}
{{#if build.baseBuild}}
  Compared to
  {{!-- NOTE: we use the baseBuild.id instead of the object to force the next build to reload so all relationships are available. TODO: better way? --}}
  {{#link-to "organization.project.builds.build" build.project.organization.slug build.project.slug build.baseBuild.id}}Build {{build.baseBuild.buildNumber}}{{/link-to}}
  (<strong>{{build.baseBuild.branch}}</strong>)
{{else}}
  {{#if build.isFinished}}
    No baseline to compare against
  {{else}}
    --
  {{/if}}
{{/if}}
{{fa-icon "compress"}}
{{#if build.baseBuild}}
  compared to {{#link-to "builds.build" build.repo.namespace.id build.repo.name build.baseBuild.id}}Build #{{build.baseBuild.buildNumber}}{{/link-to}} (<strong>{{build.baseBuild.branch}}</strong>)
{{else}}
  {{#if build.isFinished}}
    no base build to compare
  {{else}}
    --
  {{/if}}
{{/if}}
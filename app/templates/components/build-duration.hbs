{{fa-icon 'clock-o'}}

{{#if build.finishedAt}}
  {{#if build.durationHours}}{{build.durationHours}} h{{/if}}
  {{#if build.durationMinutes}}{{build.durationMinutes}} min{{/if}}
  {{#if build.durationSeconds}}{{build.durationSeconds}} sec{{/if}}
{{else}}
  --
{{/if}}

<div class="container">
  <div class="Header Header--dark">
    <div class="NavMenu">
      <div class="NavMenu-Item--left">
        {{#link-to "index"}}
          <img src="/images/percy-santa.svg" class="Header-logo u-pull-left">
          <div class="Header-title u-pull-left">
            Percy
          </div>
        {{/link-to}}
      </div>
    </div>
    <div class="u-pull-left">
      {{#if session.isAuthenticated}}
        {{organizations/switcher-nav currentOrganization=currentOrganization}}
      {{/if}}
    </div>

    {{#if session.isAuthenticated}}
      {{nav-menu user=session.currentUser currentOrganization=currentOrganization}}
    {{else}}
      {{nav-menu}}
    {{/if}}
  </div>
</div>

<form action="javascript:;">
  {{form-fields/input
      property="name"
      title="Organization name"
      changeset=changeset
      validateProperty=(action "validateProperty" changeset "name")
  }}
  {{form-fields/input
      property="slug"
      title="Slug"
      changeset=changeset
      validateProperty=(action "validateProperty" changeset "slug")
  }}
  <p>
    The slug is used to identify this organization in URLs and the Percy API. If changed, you must update the <code>PERCY_PROJECT</code> environment variable in your CI service. Note, old build URLs will break if the slug is changed and a new organization can claim the old slug.
  </p>
  {{form-fields/submit
    value="Save"
    classes="Button Button--primary"
    isSaving=isSaving
    isSaveSuccessful=isSaveSuccessful
    submit=(action "save")
  }}
</form>
{{form-fields/input
    property="name"
    title="Organization name"
    changeset=changeset
    validateProperty=(action "validateProperty" changeset "name")
    autofocus=true
    placeholder="Widgets Inc."
}}
{{form-fields/submit value="Create Organization" classes="Button--primary" submit=(action "save")}}

{{#if currentIntegration}}
  <div class="OrganizationsGithubIntegration-actions">
    <a href="{{currentIntegration.githubHtmlUrl}}" class="Button Button--withLeftIcon" target="_blank">
      {{fa-icon "github" classNames="fa--left"}}
      Manage
    </a>
  </div>
  <img class="OrganizationsGithubIntegration-leftAvatar" src="{{currentIntegration.githubAccountAvatarUrl}}&s=64">
  <header>Installed!</header>
  <p class="mt-1">
    <strong>Note:</strong> each project must be linked to a GitHub repository for {{#link-to "docs.page" "learn/github-integration"}}Pull Request integration{{/link-to}} to work. You can verify the link in each project's settings.
  </p>

{{else if organization.githubIntegrationRequest}}
  <div class="OrganizationsGithubIntegration-actions">
    <a href="#" class="Button" {{action "cancelIntegrationRequest"}}>Cancel</a>
  </div>
  {{fa-icon "fa-spinner fa-pulse" classNames="OrganizationsGithubIntegration-leftAvatar"}}
  <header>Waiting for installation...</header>
  Waiting for <strong>{{organization.githubIntegrationRequest.createdBy.login}}</strong> to install Percy in GitHub.
{{/if}}

<p>
  You can link this project to a GitHub repository. This enables Percy commit statuses on GitHub Pull Requests.
</p>
{{#if organization.isGithubIntegrated}}
  <p>
    {{saving-indicator isSaving=isSaving isSaveSuccessful=isSaveSuccessful}}
    {{#power-select
      selected=project.repo
      searchField="slug"
      placeholder="Select GitHub repository..."
      renderInPlace=true
      matchTriggerWidth=false
      allowClear=true
      options=organization.repos
      onchange=(action "chooseRepo")
      as |repos|
    }}
      {{repos.slug}}
    {{/power-select}}
  </p>
{{/if}}
